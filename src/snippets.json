{
    "Sample Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shortcdut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shortcudfast":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shortcusdfst":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shortfdafcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shortdfadfascut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sadmpole Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Saddmple Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sdample Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sadmple Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Samdple Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sampdle Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Samplde Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sampled Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample dShortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Sdhortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shdortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shodrtcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shordtcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shortdcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Saample Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Saaample Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sampale Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Samplae Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Samplea Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample aShortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Sahortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shaortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shoartcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shoratcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sample Shortacut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Shample Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Sahmple Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Samhple Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Samphle Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Samplhe Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];",
    "Quick Shortcut":"// We'll use this later to reference the output of a calculation\nlet calcVar = actionOutput();\n\n// Define a list of actions\nconst actions = [\n  comment({\n    text: 'Hello, world!',\n  }),\n  number({\n    number: 42,\n  }),\n  calculate({\n    operand: 3,\n    operation: '/',\n  }, calcVar),\n  showResult({\n    // Use the Magic Variable\n    text: withVariables`Total is ${calcVar}!`,\n  }),\n];"
}